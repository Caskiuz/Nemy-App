run = "npm run production:start"
entrypoint = "server/server-demo.ts"
modules = ["nodejs-20", "web", "nix"]

[nix]
channel = "stable-25_05"

[deployment]
run = ["sh", "-c", "npm run production:start"]
deploymentTarget = "cloudrun"

[env]
NODE_ENV = "production"
